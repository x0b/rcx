/**
 * Provides a standardized abstraction to access Android Storage Access
 * Framework targets using a subset of WebDAV APIs. This is especially useful
 * for native libraries and other non-JVM binaries where SAF is not easily
 * accessible.
 */
package io.github.x0b.safdav;